import Container from "react-bootstrap/Container"
import Row from "react-bootstrap/Row"

const Home = () =>{
    return (
        <div>
            <Container fluid>
                <Row>
                    <p>This front-end application can help you manage customers, orders, and product catalog. Please use the above Navigation bar to find the appropriate forms</p>
                </Row>
            </Container>
        </div>
    );
};

export default Home;